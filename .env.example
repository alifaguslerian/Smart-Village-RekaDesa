# Copy ke .env (jangan commit .env)
# MySQL primary (sesuai proposal, via docker-compose)
DATABASE_URL=mysql+pymysql://rekadesa:rekadesa123@localhost:3306/rekadesa?charset=utf8mb4
# Fallback lokal tanpa Docker:
# DATABASE_URL=sqlite:///./rekadesa.db
